# monolith-chi-htmx-sqlite

## 📝 アプリ概要

**目的**：シンプルなToDo管理アプリケーション（カテゴリ機能付き）
**構成**：Monolithicアーキテクチャ（サーバーサイドレンダリング）
**技術スタック**：

| 項目      | 技術                                                                           |
| ------- | ---------------------------------------------------------------------------- |
| バックエンド  | Go（[chi](https://github.com/go-chi/chi)）                                     |
| フロントエンド | HTML（+ [htmx](https://htmx.org/) + [Tailwind CSS](https://tailwindcss.com/)） |
| データベース  | SQLite                                                                       |
| テンプレート  | Go標準 `html/template`                                                         |

---

## 🧱 ER図（簡易）

```text
+---------+          +---------------+          +--------------+
|  todos  |          | todo_category |          |  categories  |
+---------+          +---------------+          +--------------+
| id      |<--+    +--| todo_id       |    +--->| id           |
| title   |   |    |  | category_id   |<---+     | title        |
| status  |   |    +---------------+            +--------------+
+---------+
```

* `todos`: タスク本体
* `categories`: カテゴリ
* `todo_category`: 多対多リレーション（中間テーブル）

---

## 📂 ディレクトリ構成（例）

```
/src
├── main.go
├── templates/
│   ├── layout.html
│   ├── index.html
│   └── edit.html
├── static/
│   └── tailwind.css（ビルド済）
├── models/
│   └── models.go
├── handlers/
│   └── todo.go
└── db/
    └── schema.sql
```

---

## 🔧 機能要件（主要）

### ✅ Todo機能

* 一覧表示（カテゴリと紐付け含む）
* 新規作成／編集／削除
* ステータス更新（例：未完了 ↔ 完了）

### ✅ カテゴリ機能

* カテゴリの追加／編集／削除
* カテゴリによる絞り込み表示

### ✅ UX向上

* htmx による非同期更新（部分的な再描画）
* Tailwind CSS で簡易UIスタイル

## design
design統一のために原則下記のTailwind テンプレート デザインではないhtml css は使用禁止
/home/wsl/dev_my_study/monolith-chi-htmx-sqlite/template-admin/html

---

## 💡 今後の展望（任意）

* 認証追加（擬似ログイン）
* カテゴリ色分け／ドラッグ＆ドロップ並び替え
* lowdb等との比較実装（Express PoC用と共通化）

---

必要であれば、SQLite初期スキーマや`main.go`のベースコード、htmxテンプレートも順次提供可能です。
この構成で進めても問題ないか、または修正要望があればご指示ください。
